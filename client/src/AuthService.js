import TokenService from './TokenService';
import AjaxAdapter  from './AjaxAdapter';

const authRoutes

const AuthService = {
  authenticate({ email, password }) {
    // req auth from the backend

    // receive token

    // save token

    // decode and return the public data to the client
  },

  register({ email, password }) {

  },

  isAllowed(roles) {

  },

  isAuthenticated() {

  },

};
